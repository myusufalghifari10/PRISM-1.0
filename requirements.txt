# BCQf Reproduction — Python Dependencies
# Install: pip install -r requirements.txt
#
# NOTE: Tang's original repo used PyTorch Lightning 1.x + numpy<2 + pandas<3.
# Our reproduction has been migrated to Lightning 2.x with newer library versions.
# These are the EXACT versions installed in prism-venv and confirmed working.

# ── PyTorch (CUDA 12.x, RTX 5060) ──
torch==2.12.1
pytorch-lightning==2.6.5
torchmetrics==1.9.0

# ── Data Processing ──
numpy==2.5.0
pandas==3.0.3
scipy==1.18.0
scikit-learn==1.5.2

# ── Database (MIMIC-III extraction) ──
psycopg2-binary==2.9.12
PyPrind==2.11.3

# ── Imputation (cohort construction) ──
# fancyimpute 0.7.0 used for legacy compatibility; sepsis_cohort.py
# was updated to prefer sklearn.impute.KNNImputer as fallback
fancyimpute==0.7.0

# ── Visualization ──
matplotlib==3.11.0
seaborn==0.13.2

# ── Utilities ──
tqdm==4.68.3
joblib==1.5.3

# ── Jupyter ──
jupyter==1.1.1
ipykernel==7.3.0
